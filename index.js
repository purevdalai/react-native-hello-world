/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import ProfileScreen from './src/components/screen/profile';

AppRegistry.registerComponent(appName, () => ProfileScreen);
